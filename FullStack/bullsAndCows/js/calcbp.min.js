"use strict";const calcbp=(selected_num,guess_num,dig=4)=>{const selected_arr=Array.from(selected_num.toString()).map(Number),guess_arr=Array.from(guess_num.toString()).map(Number);let bool=0,bool_arr=new Array(dig),pgia=0;for(let i=0;i<dig;i++)selected_arr[i]==guess_arr[i]?(bool++,bool_arr[i]=!0):bool_arr[i]=!1;for(let i=0;i<dig;i++)for(let j=0;j<dig;j++)selected_arr[i]!=guess_arr[j]||0!=bool_arr[i]||i==j||bool_arr[j]||pgia++;return[bool,pgia]};